import Regression from '../components/Regression';

# 4.

By using walk mode as the base, we obtain the following model.

<Regression
  inputs={[
    'apd',
    'opb',
    'opw',
    'opc',
    'f1gensvc',
    'f2convac',
    'f3psych',
    'cost'
  ]}
  base={1}
  table={{ title: 'Table 5. BS Model (Walk)' }}
/>

Overall the coefficients does not change a lot. However, ASCs have observed a large difference from the car-based model, which has been caused by the same reason explained in section 1.
