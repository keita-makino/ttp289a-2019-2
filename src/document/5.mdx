import Regression from '../components/Regression';

export const validators = [
  'apd',
  'opb',
  'opw',
  'opc',
  'f1gensvc',
  'f2convac',
  'f3psych',
  'cost'
];

# 5.

The following tables displays the constraint models which respectively omits "opb", "opw" and "opc".

<Regression
  inputs={['apd', 'opw', 'opc', 'f1gensvc', 'f2convac', 'f3psych', 'cost']}
  validators={validators}
  base={2}
  table={{ title: 'hoge' }}
/>
<Regression
  inputs={['apd', 'opb', 'opc', 'f1gensvc', 'f2convac', 'f3psych', 'cost']}
  validators={validators}
  base={2}
  table={{ title: 'hoge' }}
/>
<Regression
  inputs={['apd', 'opb', 'opw', 'f1gensvc', 'f2convac', 'f3psych', 'cost']}
  validators={validators}
  base={2}
  table={{ title: 'hoge' }}
/>
